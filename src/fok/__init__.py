"""FOK research project: exploring whether a language model's internal states
contain information that could be described as a "feeling of knowing".

This package provides a small, modular research pipeline:

    datasets -> extraction -> probes -> evaluation -> analysis -> plots

The command-line entry point is the ``fok`` script (see :mod:`fok.cli`).
"""

__version__ = "0.1.0"
